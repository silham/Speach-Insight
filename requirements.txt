fastapi
uvicorn
python-multipart

torch
torchaudio
transformers

openai-whisper
pyannote.audio

librosa
soundfile
ffmpeg-python
numpy
scipy

# --- Semantic Segmentation (sentence splitting) ---
nltk

tqdm
pandas

# --- Emotion Recognition (Phase 1-6) ---
vaderSentiment
# Pinned: the role-classifier artifacts in pipeline/role_classification/models
# are pickled estimators, which warn (and may break) across sklearn versions.
# Re-save them with the new version if you bump this.
scikit-learn==1.7.2

# --- RAG (Retrieval-Augmented Generation) ---
langchain
langchain-community
langchain-core
langchain-text-splitters
langchain-google-genai
chromadb
sentence-transformers
pypdf
docx2txt
python-dotenv

# --- Speaker Role Classifier Integration ---
# xgboost needs the OpenMP runtime at import time:
#   macOS: brew install libomp   |   Debian/Ubuntu: apt install libgomp1
xgboost
textblob
joblib
google-genai